#include "main.h"
/**
* _isdigit - looks for a digit from (0 through 9)
* @c: integer value to be checked
* Return: returns 1 if c is a digit,and if not 0
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
