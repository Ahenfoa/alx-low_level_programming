#include "main.h"
/**
*_memcpy - copies memory area
*@dest: where memory will be stored
*@src: where memory will be copied
*@n: no of byte
*
* Return: returns a
* copied memory with n byted changed
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
int x = 0;
int y = n;
for (; x < i; x++)
{
dest[x] = src[x];
n--;
}
return (dest);
}
