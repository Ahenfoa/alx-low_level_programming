#include "main.h"
/**
* _abs - calcualtes the absolute value of an integer
* @n: the interger value to check
* Return: returns the absolute value of int
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
} return (-n);
}
